# Run the tests

Run the repository's test suite - at minimum the tests for this task.
Report results verbatim; do not rationalize a failure away.

pass only when everything you ran is green. On red, your summary must name
the failing tests and quote the relevant errors - it is all the next fix
attempt gets to work with.
