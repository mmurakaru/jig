# Implement the feature

Build the spec from the previous handoff, test-first where the repository
has tests: a red test for each new behavior, then make it green. Match the
surrounding code's style and reuse existing helpers before writing new ones.
A previous attempt's handoff tells you what failed - revise the approach, do
not repeat it.

Done when every statement in the spec is implemented and covered by a test:
your summary maps each requirement to where it lives; changed files as
artifacts.
